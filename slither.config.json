{
  "filter_paths": ["node_modules", "build", "dist", ".next"],
  "detectors_to_exclude": [
    "assembly",
    "pragma",
    "solc-version",
    "naming-convention"
  ],
  "compilation_settings": {
    "solc_version": "0.8.24",
    "optimizer": {
      "enabled": true,
      "runs": 200
    }
  }
}
